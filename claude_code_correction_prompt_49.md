Добавлена тема PL-014 (Позитив). Данные уже обновлены — код трогать не нужно (конвертер и Journey Mode универсальны для любого числа уроков, см. правку 40). Технически всё корректно, но по содержанию нашёл 4 пересечения с уже написанными уроками — читай раздел «На заметку автору» ниже, это важнее, чем в прошлый раз.

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_11_PL014`): все 131 старых узла не тронуты (включая переписанный в правке 48 PL-013), добавлено 11 новых (`PL-HIST-0132`…`PL-HIST-0142`) для PL-014 (от прямого позитива Байяра 1839 до Polaroid и хромогенной цветной печати), плюс строка PL-014 в листе «03_Lessons_Map». Проверил вручную: ровно один флагман (`PL-HIST-0133` — соляная бумажная печать как первый практически важный позитив), источники существуют, ветки и типы узлов — из уже используемого словаря, координаты валидны (2 узла намеренно без координат).

Хорошая деталь: PL-011 ещё в самом начале явно анонсировал, что PL-013 и PL-014 вместе раскроют тему «негатив и позитив» — PL-014 логично продолжает эту связку.

Telegram-ссылка для PL-014 в xlsx пока общая (`https://t.me/photolab_vf`) — заменим на прямую, когда пришлют ссылку на пост.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py` выполнен, вывод: `OK: 142 узла`, без warning) — пересобирать заново не нужно, только проверить.

Код не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode должно быть 14 шагов маршрута. PL-014 открывается, заголовком служит «Главная тема» из xlsx («Позитив»).
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–013 (сохранены как есть)
- Код — в этой правке менять нечего

## На заметку автору (не блокирует пуш, но стоит решить)

Нашёл 4 узла PL-014, которые содержательно почти дублируют факты, уже рассказанные в других уроках:

- `PL-HIST-0135` (PL-014, альбуминовая печать, Бланкар-Эврар, Франция, 1850) — практически то же самое, теми же словами по сути, что уже есть в `PL-HIST-0126` (PL-013, тот же человек, та же дата, та же страна). Это самое сильное пересечение — дублирует узел, который я только что проверял в прошлой правке.
- `PL-HIST-0140` (PL-014, Kodachrome как reversal-позитив, 1935) — то же самое, что уже есть в `PL-HIST-0131` (PL-013, тот же факт: Kodachrome даёт позитивную прозрачность вместо негатива).
- `PL-HIST-0134` (PL-014, «The Pencil of Nature», 1844–46, Тальбот) — то же самое издание уже описано в `PL-HIST-0035` (PL-004).
- `PL-HIST-0138` (PL-014, сервисная модель Kodak «сними — остальное сделаем мы», 1888–1900) — по сути то же, что уже есть в `PL-HIST-0075` (PL-008, та же идея сервиса проявки/печати).

Слабее выраженное, но тоже стоит иметь в виду: флагман PL-014 (`0133`, соляная печать как первый практический позитив) тематически близок к узлу `PL-HIST-0124` в PL-013 (химия соляной печати) — там другой ракурс (химия процесса vs позитив как результат), это не обязательно ошибка, но эти два узла стоит перечитать рядом.

Остальные 7 узлов PL-014 (Байяр, gelatin POP-бумага, developing-out paper, Autochrome, Polaroid, chromogenic print) выглядят как новый, не пересекающийся материал.

Если решишь переписать пересекающиеся узлы (как в прошлый раз с PL-013) — просто напиши «заменить на новое», и я подготовлю новую версию этих 4 узлов с проверенными фактами и источниками, не трогая остальные 7.
