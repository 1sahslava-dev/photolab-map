Добавлены пять новых тем про типы плёночных камер: PL-024 (Дальномерная камера), PL-025 (Зеркальная камера), PL-026 (Компактная плёночная камера), PL-027 (Средний формат), PL-028 (Большой формат). Нашёл четыре дублирования — все переписаны сразу (без дополнительного вопроса, как договорились). Данные обновлены, код не менялся.

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_17_PL024-028`): все 241 старый узел не тронуты, добавлено 55 новых (`PL-HIST-0242`…`PL-HIST-0296`) — по 11 на каждый из пяти уроков, плюс пять строк в «03_Lessons_Map». Ровно один флагман на каждый урок, источники/ветки/типы узлов валидны, координаты корректны везде, где заполнены.

### Найденные и переписанные дубли

1. **`PL-HIST-0244` (флагман PL-024)** дословно повторял уже существующий `PL-HIST-0098` (PL-010): «Leica II — первая Leica со встроенным дальномером». Переписал на более глубокий факт — как физически работает совмещённый дальномер (триангуляция, эффективная база = расстояние между окнами × увеличение), Leica II осталась примером, но уже не как самоцель. Источник заменён на Wikipedia — Coincidence rangefinder (`SRC-238`).
2. **`PL-HIST-0253`** (не флагман, открывающий узел PL-025) в третий раз повторял связку «зеркало + фокусировочный экран + пентапризма показывают кадр через объектив» (уже было в `PL-HIST-0216` PL-021 и `PL-HIST-0227` PL-022). Переписал на конкретный факт, которого раньше не было: зачем вообще нужна пентапризма — она через несколько внутренних отражений исправляет зеркальный (левый-правый) переворот кадра, который дало бы обычное плоское зеркало. Источник — Wikipedia — Pentaprism (`SRC-239`).
3. **`PL-HIST-0265`** (PL-026) повторял `PL-HIST-0088` (PL-009): «Kodak Retina — дневная зарядка 135-картриджа». Переписал на другой факт о той же камере — складной корпус на мехах (bellows) как путь к компактности, наследие довоенных камер Nagel Vollenda. Источник (Science Museum Group) оставил тот же — он адекватно покрывает и эту сторону камеры. Заодно поправил связку в соседнем узле `PL-HIST-0266`: «Предыдущая связь» была «Kodak Retina и кассетный 35 мм», стала «Kodak Retina и складной корпус».
4. **`PL-HIST-0275` (флагман PL-027)** дословно повторял `PL-HIST-0072` (PL-008): «No.2 Brownie вводит 120 roll film — основа среднего формата». Переписал на факт, которого раньше не было: как именно 120-плёнка позволяла заряжаться при свете — бумажная подложка с напечатанными номерами кадров, видимыми через красное окошко на задней стенке камеры. Источник заменён на Wikipedia — 120 film (`SRC-240`), заодно снял пометку «Проверить позже» — она относилась именно к старой формулировке.

Остальные 51 новый узел без пересечений — специально сверил дальномерную/SLR-линию с PL-001, PL-010, PL-021, PL-022 и средний/большой формат с PL-007–009: пересечений по темам много (это ожидаемо, все уроки про камеры), но по конкретным фактам — только эти четыре случая.

16 узлов из новых намеренно без координат (в т.ч. флагман PL-024 после правки по-прежнему с координатами, а вот `PL-HIST-0286`, флагман PL-028 — без координат, тот же случай, что раньше был с PL-015/019/021, и код уже умеет корректно нумеровать шаги маршрута в этой ситуации, правка 54).

`src/data/data.json` пересобран (`python3 scripts/convert.py`, вывод: `OK: 296 узлов`, без warning; в Journey Mode теперь 28 шагов) — пересобирать заново не нужно, только проверить.

Код не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode должно быть 28 шагов маршрута.
   - Шаг 24 (PL-024, Leica II): текст должен объяснять триангуляцию и эффективную базу дальномера, а не «первая Leica со встроенным дальномером».
   - Шаг 28 (PL-028, флагман без координат): маркера на карте не будет, правая панель должна показывать содержимое корректно, маршрут перескакивает от станции 27 к 28 без сдвига номеров.
3. `npm run build` — сборка без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–023 (сохранены как есть)
- Код — в этой правке менять нечего
