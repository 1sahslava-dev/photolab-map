Добавлена тема PL-012 (эмульсия: как свет становится изображением). Данные уже обновлены — код трогать не нужно (конвертер и Journey Mode универсальны для любого числа уроков, см. правку 40).

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_9_PL012`): все 109 старых узлов не тронуты, добавлено 11 новых (`PL-HIST-0110`…`PL-HIST-0120`) для PL-012 (от желатино-бромидной эмульсии Мэддокса 1871 до панхроматической чувствительности и роли размера кристаллов серебра), плюс строка PL-012 в листе «03_Lessons_Map». Проверил вручную: ровно один флагман (`PL-HIST-0110` — публикация Мэддокса об эмульсии, 1871), источники существуют, ветки (в т.ч. новая для этого урока — «Цвет», уже была в словаре) и типы узлов — из уже используемого словаря, координаты валидны. 8 из 11 узлов намеренно без координат («распределённый технический принцип», «тёмная комната / лаборатория», «производственные центры» — базовая химия эмульсии без единой точки на карте), тот же приём, что и в предыдущих уроках.

Хорошая деталь: флагман прямо продолжает то, на что PL-011 уже ссылался вперёд («PL-012: эмульсия как главный светочувствительный слой») — преемственность сохранена, дублей с PL-011 не заметил.

Telegram-ссылка для PL-012 в xlsx пока общая (`https://t.me/photolab_vf`, старый канал) — заменим на прямую ссылку в группу `photolab_vf_pro`, когда пришлют ссылку на пост.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py` выполнен, вывод: `OK: 120 узлов`, без warning) — пересобирать заново не нужно, только проверить.

Код (`scripts/convert.py`, `src/App.jsx`, `NodeCard.jsx`, `JourneySteps.jsx`) не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode должно быть 12 шагов маршрута. PL-012 открывается, заголовком служит «Главная тема» из xlsx («Эмульсия: как свет становится изображением»), т.к. отдельной редакционной строки для него ещё нет.
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–011 (сохранены как есть)
- Код — в этой правке менять нечего
