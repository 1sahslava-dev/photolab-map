Переписал первую половину PL-013, которая дублировала PL-004 (см. предыдущую версию этого файла). Данные уже обновлены — код трогать не нужно.

## Что изменено

`data/master.xlsx`, лист «01_Master_Facts» — 5 узлов PL-013 (`PL-HIST-0121`…`PL-HIST-0125`) переписаны с нуля, чтобы не повторять PL-004. Остальные 6 узлов PL-013 (`0126`…`0131`: альбуминовая печать, мокрый коллодий, carte-de-visite, Kodak, желатиновая бумага, Kodachrome) и все 120 узлов других уроков не тронуты.

Новый сюжет первой половины — вместо повтора биографии Тальбота (уже рассказана в PL-004), теперь это история самой системы «негатив-позитив» как концепции и её судьбы:

- `0121` — что вообще значит «негатив» (инверсия тонов, идея тиражируемой матрицы) — концептуальный узел, без даты/точки на карте.
- `0122` — **новый флагман PL-013**: январь 1839, гонка приоритета. Тальбот, узнав об анонсе Дагера в Париже, спешно показывает «фотогенные рисунки» в Royal Institution в Лондоне (25 января) как конкурирующую воспроизводимую систему. Это другой факт и другой ракурс, чем у PL-003 (там про сторону Дагера) и PL-004 (там про сам патент калотипа 1841).
- `0123` — патент Тальбота тормозит коммерческое распространение калотипа в Британии (лицензия до £300/год), в отличие от бесплатного дагеротипа во Франции.
- `0124` — химия соляной бумажной печати (printing-out process: бумага с хлоридом серебра темнеет на солнце без проявителя) — технический факт, а не биографический.
- `0125` — Тальбот не получает патент во Франции (Париж, 1843) — мостик к уже существующему узлу `0126` (Бланкар-Эврар, Франция, 1850): там процесс можно свободно улучшать без лицензии.

Проверил: дублей с PL-004 (Тальбот/калотип) и PL-003 (Дагер) больше нет, флагман только один на PL-013 (`0122`), ветки и типы узлов — из существующего словаря, источники (`SRC-088`, `SRC-094` новый, `SRC-095` новый) прописаны и добавлены в `04_Sources`:

- `SRC-094` — The Met, эссе про Тальбота (гонка 1839, патент, поездка в Париж 1843)
- `SRC-095` — National Gallery of Canada, про химию calotype/соляной печати

`src/data/data.json` уже пересобран (`python3 scripts/convert.py`, вывод: `OK: 131 узел`, без warning) — пересобирать заново не нужно, только проверить.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode PL-013 теперь флагманом должен быть новый узел про гонку 1839 года (январь), а не патент калотипа 1841 (тот остался флагманом PL-004, как и раньше).
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–012 и узлы `0126`–`0131` внутри PL-013 (сохранены как есть)
- Код — в этой правке менять нечего
