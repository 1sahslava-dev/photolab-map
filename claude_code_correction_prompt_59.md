Добавлены три новые темы: PL-021 (Из чего состоит плёночная камера), PL-022 (Механическая камера), PL-023 (Камера с автоматикой). Данные уже обновлены — код трогать не нужно. Дублирования с прошлыми уроками не нашёл, переписывать было нечего.

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_16_PL021-023`): все 208 старых узлов не тронуты, добавлено 33 новых (`PL-HIST-0209`…`PL-HIST-0241`) — по 11 на каждый из трёх уроков, плюс три строки в «03_Lessons_Map». Проверил вручную: ровно один флагман на каждый урок (`PL-HIST-0209`, `PL-HIST-0220`, `PL-HIST-0231`), источники существуют, ветки и типы узлов — из уже используемого словаря, координаты корректны везде, где заполнены.

Эта тройка уроков впервые заходит в новую тему — устройство и механику самой камеры (корпус, диафрагма, затвор, видоискатель, экспонометр, потом механическое управление, потом автоматика Canon/Nikon/Minolta). Специально сверил с уже написанными уроками (PL-001 про камеру-обскуру, PL-010 про Leica и сменную оптику, PL-018 про экспозицию, PL-020 про экспонометрию) — пересечений по конкретным фактам нет, только соседние, но разные темы (например, Leica screw-mount в PL-010 и байонет Nikon F в PL-021 — это два разных факта из истории сменной оптики, не повтор). Автор просил заменить дубликаты, если найдутся, — в этот раз находить было нечего.

16 новых узлов намеренно без координат (в т.ч. флагман PL-021, `PL-HIST-0209`) — это узлы про устройство камеры вообще, не про конкретное историческое место; тот же случай, что раньше был с флагманами PL-015 и PL-019, и код уже умеет корректно нумеровать шаги маршрута в этой ситуации (правка 54) — сдвига номеров на карте не будет.

Telegram-ссылки для PL-021–023 в xlsx указывают на общую группу (`https://t.me/photolab_vf_pro`, без номера поста) — заменим на прямые посты, когда пришлют ссылки.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py`, вывод: `OK: 241 узел`, без warning; в Journey Mode теперь 23 шага) — пересобирать заново не нужно, только проверить.

Код не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode должно быть 23 шага маршрута. Проверь шаг 21 (PL-021, флагман без координат) — на карте у него не будет своего маркера, правая панель должна корректно показывать содержимое; линия маршрута перескакивает от станции 20 к 22, не ломая нумерацию соседних маркеров (21→22→23 без сдвига).
3. `npm run build` — сборка без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–020 (сохранены как есть)
- Код — в этой правке менять нечего
