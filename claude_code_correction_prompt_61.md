Добавлены пять новых тем — впервые не исторических, а практических/образовательных: PL-029 (Как выбрать первую плёночную камеру), PL-030 (Почему камера не делает фотографию за автора), PL-031 (Что такое объектив), PL-032 (Из чего состоит объектив), PL-033 (Фокусное расстояние). Нашёл одно дублирование, переписал сразу. Данные обновлены, код не менялся.

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_18_PL029-033`): все 296 старых узлов не тронуты, добавлено 55 новых (`PL-HIST-0297`…`PL-HIST-0351`) — по 11 на каждый из пяти уроков, плюс пять строк в «03_Lessons_Map». Ровно один флагман на каждый урок, источники/ветки/типы узлов валидны.

### Важная особенность этой партии — на заметку автору (не блокирует пуш)

Все 55 новых узлов, включая все пять флагманов, — **без координат**. Это не ошибка и не пропуск: в отличие от PL-001–028 (исторические факты, привязанные к конкретному месту и году), эти пять уроков — практические/концептуальные («как выбрать камеру», «что такое объектив», «фокусное расстояние») без единой исторической точки на карте. Раньше по одному такому узлу без координат уже было в PL-015/019/021/028 (и правка 54 научила код корректно вести нумерацию шагов маршрута в этой ситуации), но здесь **впервые целых пять уроков подряд полностью без единой точки на карте**.

Практическое следствие: в Journey Mode шаги 29–33 не будут иметь ни одного маркера на карте — маршрут визуально «перепрыгнет» напрямую от станции 28 (PL-028) к тому месту, где появится следующая историческая тема (если такая будет). Правая панель при этом покажет содержимое всех пяти уроков корректно. Технически всё работает без сбоев, но стоит решить: устраивает ли тебя, что «Историческая карта фотографии» теперь включает уроки без исторической составляющей вообще — возможно, для таких практических тем в будущем имеет смысл отдельный не-картографический раздел сайта, а не встраивание в Journey Mode карты. Сейчас ничего не меняю, просто фиксирую наблюдение.

### Найденное дублирование (переписано)

**`PL-HIST-0323`** (PL-031, не флагман) почти дословно повторял `PL-HIST-0213` (PL-021): «диафрагма — регулируемое отверстие, меняет количество света, влияет на глубину резкости». Переписал на факт, которого в базе ещё не было: диафрагменное число f — это не абсолютный размер, а отношение фокусного расстояния к диаметру входного отверстия (f/N = фокусное расстояние ÷ диаметр диафрагмы), поэтому f/2.8 на разных объективах — разные физические отверстия при одинаковой доле света. Источник заменён на Wikipedia — F-number (`SRC-241`). Остальные узлы PL-031/032 про диафрагму (`0326` — светосила и практический смысл числа f, `0334` — лепестки диафрагмы как механизм) не пересекаются с этим фактом и друг с другом — три разных грани одной темы, не повтор.

Остальные 54 новых узла без пересечений — специально сверил объективную анатомию (элементы, байонет, покрытия) с PL-021/022/024 (диафрагма, байонет Nikon F, дальномер) — пересечений по конкретным фактам, кроме указанного выше, не нашёл.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py`, вывод: `OK: 351 узел`, без warning; в Journey Mode теперь 33 шага) — пересобирать заново не нужно, только проверить.

Код не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`. (Увидишь `claude_code_correction_prompt_58.md` и `_59.md` как удалённые — не относится к этой правке, файлы отсутствовали на диске ещё до неё.)
2. `npm run dev` — в Journey Mode должно быть 33 шага маршрута. Шаги 29–33 — без маркеров на карте (см. пояснение выше), маршрут визуально пропускает эти станции, но правая панель и лента карточек внизу должны работать нормально для всех пяти.
3. `npm run build` — сборка без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–028 (сохранены как есть)
- Код — в этой правке менять нечего
