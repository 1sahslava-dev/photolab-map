Explore Mode: карта была слишком тёмной. Правка уже внесена в файлы (см. ниже) — нужно проверить, собрать и задеплоить. Journey Mode и остальной интерфейс не трогать.

## Что изменено

`src/theme.css`, блок Explore Mode:

- `--explore-land`: `#2a2218` → `#423a2c`
- `--explore-coast`: `#4a3e2e` → `#6a5c46`
- добавлена новая переменная `--explore-ocean: #16233c` (раньше вода карты рисовалась через `--ink`, но `--ink` — это ещё и цвет текста в светлой теме, трогать его напрямую нельзя)

`src/App.css`:

- `.app-shell[data-mode="explore"] .app-map { background: var(--ink); }` → `background: var(--explore-ocean);`
- `.flatmap-bg { fill: var(--ink); }` → `fill: var(--explore-ocean);`

## Что нужно сделать

1. `npm run dev`, открой Explore Mode, проверь, что вода и суша на карте стали на пару тонов светлее (не радикально — просто читаемее), кромка берега (`--explore-coast`) видна на суше.
2. Убедись, что светлая тема Journey Mode не изменилась (там `--ink` используется для текста, его не трогали).
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Если всё ок — закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужен.

## Что не менять

- Journey Mode (светлая тема, `--ink`, `--pl-*` переменные)
- Логику данных, фильтров, слоёв карты
- Любые другие цвета в Explore Mode, кроме трёх выше

Пришли скриншот Explore Mode (десктоп) после правки — сравнить с текущим видом.
