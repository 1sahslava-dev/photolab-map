Добавлена тема PL-011 (плёнка как светочувствительный материал). Данные уже обновлены — код трогать не нужно (конвертер и Journey Mode универсальны для любого числа уроков, см. правку 40).

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_8_PL011`): все 98 старых узлов не тронуты, добавлено 11 новых (`PL-HIST-0099`…`PL-HIST-0109`) для PL-011 (от прозрачной плёночной основы Kodak 1889 до устройства эмульсии: галогениды серебра, скрытое изображение, служебные слои плёнки), плюс строка PL-011 в листе «03_Lessons_Map». Проверил вручную: ровно один флагман (`PL-HIST-0099` — Kodak Transparent Film, 1889), источники существуют, ветки и типы узлов — из уже используемого словаря, координаты валидны. 6 из 11 узлов намеренно без координат («распределённый процесс» / «без единой точки», страна «глобально») — это базовые понятия о плёнке (эмульсия, скрытое изображение, зерно), у которых нет одной географической точки, тот же приём, что и в предыдущих уроках.

Хорошая деталь: несколько узлов PL-011 явно ссылаются вперёд на будущие темы (PL-012 — эмульсия, PL-013/014 — негатив и позитив, PL-018 — ISO, PL-019 — зерно плёнки) — сюжет заранее размечен, дублей с PL-010 не заметил (в отличие от прошлой правки, где PL-009/010 частично пересекались).

Telegram-ссылка для PL-011 в xlsx пока общая (`https://t.me/photolab_vf`, старый канал) — заменим на прямую ссылку в группу `photolab_vf_pro`, когда пришлют ссылку на пост.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py` выполнен, вывод: `OK: 109 узлов`, без warning) — пересобирать заново не нужно, только проверить.

Код (`scripts/convert.py`, `src/App.jsx`, `NodeCard.jsx`, `JourneySteps.jsx`) не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode должно быть 11 шагов маршрута. PL-011 открывается, заголовком служит «Главная тема» из xlsx («Плёнка как светочувствительный материал»), т.к. отдельной редакционной строки для него ещё нет.
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–010 (сохранены как есть)
- Код — в этой правке менять нечего
