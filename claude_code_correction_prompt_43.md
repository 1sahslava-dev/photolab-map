Добавлена тема PL-009 (35 мм / Ur-Leica). Данные уже обновлены — код трогать не нужно (правка 40 уже сделала конвертер и Journey Mode универсальными для любого числа уроков).

## Что изменено

`data/master.xlsx` заменён на новую версию (`v0_6_PL009`): все 78 старых узлов не тронуты, добавлено 10 новых (`PL-HIST-0079`…`PL-HIST-0088`) для PL-009 (от прозрачной киноплёнки Eastman и Kinetoscope Эдисона до прототипа Ur-Leica Оскара Барнака), плюс строка PL-009 в листе «03_Lessons_Map». Проверил вручную: ровно один флагман (`PL-HIST-0084`, Ur-Leica), источники существуют, ветки и типы узлов — из уже используемого словаря, координаты валидны, 1 узел намеренно без координат («международный процесс» — как и другие распределённые узлы). Сюжет логично продолжает PL-008 (тот явно ссылался вперёд на «PL-009: 35 мм, Leica»).

Telegram-ссылка для PL-009 в xlsx пока общая (`https://t.me/photolab_vf`, старый канал) — заменим на прямую ссылку в группу `photolab_vf_pro`, когда пришлют ссылку на пост.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py` выполнен, вывод: `OK: 88 узлов`, без warning) — пересобирать заново не нужно, только проверить.

Код (`scripts/convert.py`, `src/App.jsx`, `NodeCard.jsx`, `JourneySteps.jsx`) не менялся — он уже поддерживает добавление уроков без правок кода (см. правку 40).

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json` (плюс, возможно, увидишь ещё не закоммиченное удаление старого `claude_code_correction_prompt_38.md` и новый `claude_code_correction_prompt_42.md` — не относится к этой правке, можно закоммитить заодно, не влияет на код).
2. `npm run dev` — в Journey Mode должно быть 9 шагов маршрута. PL-009 открывается, заголовком служит «Главная тема» из xlsx («35 мм»), т.к. отдельной редакционной строки для него ещё нет.
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–008 (сохранены как есть)
- Код — в этой правке менять нечего
