Переписан флагман PL-019 — устранено содержательное пересечение с PL-011/PL-012, замеченное в прошлой правке. Остальные данные PL-018–020 не менялись, код не менялся.

## Что изменено

`data/master.xlsx`, узел `PL-HIST-0187` (флагман PL-019, «Зерно»):

Старая версия повторяла (уже третий раз в базе) мысль «размер и распределение кристаллов в эмульсии определяют чувствительность/контраст/зерно» — то же самое уже было в `PL-HIST-0108` (PL-011) и `PL-HIST-0120` (PL-012).

Новая версия уходит глубже в саму тему «зерно», а не повторяет общий принцип эмульсии: раскрывает, что видимое зерно на отпечатке/скане — это не сам микроскопический кристалл серебра (кристаллы слишком малы, чтобы их различить глазом), а оптический эффект — статистическое скопление проявленного серебра или облаков красителя, которые перекрываются между собой. Это отдельный, ранее не сказанный в базе факт (в старой версии он был только в поле «Проверить позже» как незакрытый вопрос — теперь он раскрыт и стал сутью флагмана). Добавлено понятие RMS granularity — зернистость можно измерить числом, а не только оценить на глаз.

Добавлен источник `SRC-166` (Wikipedia — Film grain, https://en.wikipedia.org/wiki/Film_grain) — общий образовательный источник, содержание сверено вручную.

Обновлена связка в соседнем узле `PL-HIST-0189` (не флагман): поле «Предыдущая связь» — было «Зерно как материальная структура» (ссылалось на старую формулировку флагмана), стало «Зерно как оптический эффект скоплений серебра» (ссылается на новую). Сам узел `0189` (про ILFORD PAN F PLUS) по содержанию не менялся.

`src/data/data.json` пересобран (`python3 scripts/convert.py`, вывод: `OK: 208 узлов`, без warning) — пересобирать заново не нужно, только проверить.

Код не менялся — правок не требуется.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — открой Journey Mode, шаг 19 (PL-019): заголовком и главной идеей должен быть новый текст про зерно как оптический эффект (не «кристаллы формируют эмульсию»), в блоке «Почему это важно» — упоминание микроскопического кристалла / проявленного серебра / видимого зерна как трёх разных вещей.
3. `npm run build` — сборка без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map` **вместе с** прошлой правкой (данные PL-018–020, ещё не запушены — автор попросил переписать флагман и запушить всё разом).

## Что не менять

- Данные PL-001–018, PL-020 — не трогались.
- Остальные узлы PL-019 (`0188`–`0197`), кроме отмеченного поля связки в `0189` — не трогались.
- Код — в этой правке менять нечего.
