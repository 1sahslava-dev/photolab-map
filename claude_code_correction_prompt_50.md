Переписал 4 узла PL-014, которые дублировали уже существующие уроки (см. предыдущую версию этого файла). Данные уже обновлены — код трогать не нужно. Вы уже пушили предыдущую версию PL-014 — это обновление поверх неё, нужно закоммитить и запушить заново.

## Что изменено

`data/master.xlsx`, лист «01_Master_Facts» — 4 узла PL-014 переписаны с нуля, чтобы не повторять другие уроки. Остальные 7 узлов PL-014 (`0132`, `0133`-флагман, `0136`, `0137`, `0139`, `0141`, `0142`) и все 131 узлов других уроков не тронуты.

- `PL-HIST-0134` (было: «The Pencil of Nature», дублировало PL-004) → теперь: золотое тонирование (Физо, 1840, для дагеротипов → перенесено на бумажные позитивы к 1847) — про стойкость и цвет позитива.
- `PL-HIST-0135` (было: альбуминовая печать Бланкар-Эврара 1850, дублировало PL-013) → теперь: угольная печать (Пуатвен, 1855) — пигментный, практически не выцветающий позитив, отвечает на проблему, которую золотое тонирование только смягчало.
- `PL-HIST-0138` (было: сервисная модель Kodak, дублировало PL-008) → теперь: платинотипия (Уиллис, патент 1873 / коммерческая бумага 1880) — позитив с исключительным тональным диапазоном, который до сих пор используют художники.
- `PL-HIST-0140` (было: Kodachrome-позитив, дублировало PL-013) → теперь: Kodak Dye Transfer (1945/1946) — имбибиционная цветная печать на бумаге, явно отличена в тексте от Kodachrome (та прозрачность для проекции, эта — бумажный отпечаток).

Заодно поправил «Следующая связь» у двух неизменённых соседних узлов (`0133`, `0139`), которые раньше явно называли удалённый контент («Pencil of Nature», «Kodachrome») — теперь ссылаются на новые соседние узлы, цепочка связей снова читается связно.

Добавлены 4 новых источника в `04_Sources` (`SRC-105`…`SRC-108`): The Art of the Photogravure (Физо), Royal Collection Trust (угольная печать), Historic New Orleans Collection (платинотипия), George Eastman Museum (Dye Transfer). Все факты проверены поиском перед записью.

`src/data/data.json` уже пересобран (`python3 scripts/convert.py`, вывод: `OK: 142 узла`, без warning) — пересобирать заново не нужно, только проверить.

## Что нужно сделать

1. `git status` — должны быть изменены только `data/master.xlsx` и `src/data/data.json`.
2. `npm run dev` — в Journey Mode флагман PL-014 остался прежним (`0133`, соляная печать), но карточки узлов вокруг него теперь про золотое тонирование → угольную печать → платинотипию → Dye Transfer вместо повторов из PL-004/008/013.
3. `npm run build` — проверь, что сборка проходит без ошибок.
4. Закоммить и запушь в `1sahslava-dev/photolab-map`. Автодеплой на Vercel настроен, вручную `vercel --prod` не нужно.

## Что не менять

- Данные PL-001–013 и узлы `0132, 0133, 0136, 0137, 0139, 0141, 0142` внутри PL-014 (сохранены как есть)
- Код — в этой правке менять нечего
